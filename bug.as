function myFunction():void {
  //This is a common error when using the ActionScript 3.0 compiler, the compiler will throw an error if the variable is not initialized.
  var myVar:String;
  trace(myVar);
}