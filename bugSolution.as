function myFunction():void {
  // Initialize the variable before using it
  var myVar:String = "";
  trace(myVar);
}  //or
function myFunction():void {
  // Initialize the variable before using it to null
  var myVar:String = null;
  trace(myVar); 
}